<div class="routes">
    <table>
        <thead>
            <tr>
                <td>{{"Locale"}}</td>
                <td>{{"URL path"}}</td>
                <td>{{"Title"}}</td>
            </tr>
        </thead>
        {% for route in doc.pod.router.routes.paths %}
            <tr>
                <td>{{route}}
            </tr>
        {% endfor %}
    </table>
</div>